@{
    ViewData["Title"] = "Client Dashboard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid px-4 py-5 bg-light-subtle">
    <div class="row align-items-center mb-5">
        <div class="col">
            <h6 class="text-primary fw-bold text-uppercase mb-1" style="letter-spacing: 1px;">Overview</h6>
            <h1 class="h2 fw-bold text-dark">Welcome Back, @ViewBag.Client?.Nom! 👋</h1>
        </div>
        <div class="col-auto">
            <a asp-action="CreateRequest" class="btn btn-primary btn-lg rounded-pill shadow-sm px-4">
                <i class="bi bi-plus-lg me-2"></i>New Request
            </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-elevated alert-success d-flex align-items-center mb-4 border-0 shadow-sm" role="alert">
            <i class="bi bi-check-circle-fill me-3 fs-4"></i>
            <div>@TempData["Success"]</div>
            <button type="button" class="btn-close ms-auto" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row g-4 mb-5">
        <div class="col-md-3">
            <div class="card border-0 shadow-sm stat-card h-100">
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div class="stat-icon-wrap bg-primary-soft">
                            <i class="bi bi-collection-fill text-primary"></i>
                        </div>
                        <span class="badge rounded-pill bg-light text-dark border">Total</span>
                    </div>
                    <h2 class="fw-bold mb-1">@ViewBag.TotalPrestations</h2>
                    <p class="text-muted small mb-0">Total Bookings</p>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card border-0 shadow-sm stat-card h-100">
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div class="stat-icon-wrap bg-success-soft">
                            <i class="bi bi-check-all text-success"></i>
                        </div>
                        <span class="text-success small fw-bold">Done</span>
                    </div>
                    <h2 class="fw-bold mb-1">@ViewBag.CompletedPrestations</h2>
                    <p class="text-muted small mb-0">Completed Services</p>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card border-0 shadow-sm stat-card h-100">
                <div class="card-body p-4">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div class="stat-icon-wrap bg-warning-soft">
                            <i class="bi bi-lightning-charge-fill text-warning"></i>
                        </div>
                        <span class="text-warning small fw-bold">Active</span>
                    </div>
                    <h2 class="fw-bold mb-1">@((ViewBag.ActivePrestations as List<GestionPrestation.Models.Prestation>)?.Count ?? 0)</h2>
                    <p class="text-muted small mb-0">Prestations In Progress</p>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card border-0 shadow-sm stat-card h-100 bg-gradient-dark text-white">
                <div class="card-body p-4 text-center d-flex flex-column justify-content-center">
                    <div class="avatar-lg bg-white bg-opacity-10 rounded-circle mx-auto mb-2 d-flex align-items-center justify-content-center">
                        <i class="bi bi-person fs-2"></i>
                    </div>
                    <h5 class="fw-bold mb-0">@ViewBag.Client?.Nom</h5>
                    <p class="opacity-75 small">Premium Client</p>
                </div>
            </div>
        </div>
    </div>

    <div class="section-card bg-white rounded-4 shadow-sm mb-5 border-0 overflow-hidden">
        <div class="p-4 border-bottom d-flex justify-content-between align-items-center">
            <h5 class="fw-bold mb-0 text-dark">Current Operations</h5>
            <a asp-action="MyPrestations" class="btn btn-sm btn-light rounded-pill border px-3">View Full List</a>
        </div>

        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light-subtle">
                    <tr class="text-muted small text-uppercase">
                        <th class="px-4 py-3">Service Details</th>
                        <th>Professional</th>
                        <th>Progress Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (ViewBag.ActivePrestations != null && (ViewBag.ActivePrestations as List<GestionPrestation.Models.Prestation>).Any())
                    {
                        @foreach (var p in (ViewBag.ActivePrestations as List<GestionPrestation.Models.Prestation>))
                        {
                            <tr>
                                <td class="px-4 py-4">
                                    <div class="d-flex align-items-center">
                                        <div class="service-initial me-3 bg-primary text-white">@p.Service?.Nom[0]</div>
                                        <div>
                                            <div class="fw-bold text-dark">@p.Service?.Nom</div>
                                            <div class="text-muted small">#REF-@p.Id</div>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-person-badge me-2 text-primary"></i>
                                        <span class="small fw-medium">@(p.Prestataire != null ? $"{p.Prestataire.Nom} {p.Prestataire.Prenom}" : "Pending Assignment")</span>
                                    </div>
                                </td>
                                <td style="width: 250px;">
                                    @{
                                        var prog = GetProgress(p.Statut);
                                    }
                                    <div class="d-flex justify-content-between mb-1">
                                        <span class="badge @GetStatusBadgeClass(p.Statut) rounded-pill px-2">@p.Statut</span>
                                        <span class="small text-muted">@prog%</span>
                                    </div>
                                    <div class="progress rounded-pill" style="height: 6px;">
                                        <div class="progress-bar rounded-pill bg-primary" style="width: @prog%"></div>
                                    </div>
                                </td>
                                <td class="text-end px-4">
                                    <a asp-action="PrestationDetails" asp-route-id="@p.Id" class="btn btn-icon btn-white border shadow-sm rounded-circle">
                                        <i class="bi bi-arrow-right-short text-primary fs-4"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr><td colspan="4" class="text-center py-5 text-muted">No active prestations found.</td></tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<style>
    :root {
        --primary-soft: #eef2ff;
        --success-soft: #ecfdf5;
        --warning-soft: #fffbeb;
        --dark-bg: #1e293b;
    }

    body {
        background-color: #fcfcfd;
    }

    /* Card Styling */
    .stat-card {
        transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1);
        border: 1px solid rgba(0,0,0,0.03) !important;
    }

        .stat-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px rgba(0,0,0,0.06) !important;
        }

    .stat-icon-wrap {
        width: 45px;
        height: 45px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 12px;
        font-size: 1.2rem;
    }

    /* Soft Colors */
    .bg-primary-soft {
        background-color: var(--primary-soft);
    }

    .bg-success-soft {
        background-color: var(--success-soft);
    }

    .bg-warning-soft {
        background-color: var(--warning-soft);
    }

    .bg-gradient-dark {
        background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
    }

    /* Service Icon Avatar */
    .service-initial {
        width: 40px;
        height: 40px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        font-size: 1.1rem;
    }

    /* Table Improvements */
    .table thead th {
        border: none;
        font-weight: 700;
        color: #64748b;
        font-size: 0.75rem;
        letter-spacing: 0.5px;
    }

    .btn-icon {
        width: 36px;
        height: 36px;
        padding: 0;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .progress {
        background-color: #f1f5f9;
    }
</style>

@functions {
    string GetStatusBadgeClass(string status) => status switch
    {
        "En cours" => "bg-primary-soft text-primary border border-primary-subtle",
        "Terminée" => "bg-success-soft text-success border border-success-subtle",
        "Assignée" => "bg-info-soft text-info border border-info-subtle",
        "Annulée" => "bg-danger-soft text-danger border border-danger-subtle",
        _ => "bg-light text-dark border"
    };

    int GetProgress(string status) => status switch
    {
        "Planifiée" => 15,
        "Assignée" => 40,
        "En cours" => 75,
        "Terminée" => 100,
        _ => 0
    };
}